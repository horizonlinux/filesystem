# filesystem

# Archived
This repo was archived due to archivization of `horizon-arch` and `horizon-arch-arm64`

Base Horizon files - A fork of [Arch Linux's filesystem package](https://gitlab.archlinux.org/archlinux/packaging/packages/filesystem)
